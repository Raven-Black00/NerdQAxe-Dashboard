# ᛈ RavenMiner HQ — Requirements
# v5.9.2.0 · Thurisaz
# These are the provisions. Install them before the journey begins.
#
# pip install -r requirements.txt

# HTTP communication with NerdQAxe+ REST API and CoinGecko BTC price feed
requests>=2.28.0

# Image handling: Valknut animation, Vegvisir watermark, raven heads, icon, tray icon
Pillow>=9.0.0

# System tray icon and Windows balloon notifications
# Gracefully absent on unsupported platforms — all core features remain functional
pystray>=0.19.0
